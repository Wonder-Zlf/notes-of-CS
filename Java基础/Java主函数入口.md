# Java主函数入口

```
public class MainTest {
    /**
    * public 权限修饰符，作为主函数必须能被外部访问到
    * static 一旦类在JVM中被初始化，不允许再修改
    * void 主函数没有返回值
    * main 规定的标记，JVM中以此为类的入口函数
    * String 字符串具有被任意解释的特点，兼容各种入参
    * args 参数的变量名，原本是任意的，成习惯了
    */
    public static void main(String[] args){
    }
}

```

